import { BuildOrderForm } from '../../components/build-orders/form/BuildOrderForm';
import { Container } from '../../components/Container';

const BuildOrderCreate = (): JSX.Element => {
  return (
    <Container>
      <BuildOrderForm />
    </Container>
  );
};

export default BuildOrderCreate;
